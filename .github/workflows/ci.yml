name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.11

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Force pin Werkzeug (fix Flask test client)
        run: |
          python -m pip install Werkzeug==2.3.7

      - name: Run Pytest
        run: |
          echo "Adding repo root to PYTHONPATH"
          export PYTHONPATH=$(pwd)
          pytest -q --tb=short --disable-warnings

      - name: Build Docker image
        run: |
          docker build -t ${{ secrets.DOCKERHUB_USER }}/aceest-fitness:ci-${{ github.run_id }} .
